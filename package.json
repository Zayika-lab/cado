{
  "name": "cado-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "serve": "http-server -p 5173 -c-1 -s .",
    "emu": "firebase emulators:start --only auth,firestore,storage --project cado-test",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "firebase-tools": "^15.15.0",
    "http-server": "^14.1.1",
    "playwright": "^1.59.1"
  }
}
